//import liraries
import FilterModal from '@/components/AllComponent/FilterModal/FilterModal';
import React, { Component } from 'react';
import { View, Text, StyleSheet } from 'react-native';

// create a component
const Meal = () => {
    return (
        <View>
           <FilterModal/>
        </View>
    );
};



//make this component available to the app
export default Meal;
